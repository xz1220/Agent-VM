name: bad-agent
version: "1.0.0"
source_scope: global
runtime:
  preferred: codex
  kind: local
  mode: primary
permissions:
  approval: on-request
  sandbox: workspace-write
workspace_isolation:
  enabled: true
